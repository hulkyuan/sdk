/**
 * 通用请求报文
 */
interface IRequest {
    /**
     * 签名数据
     * 根据签名算法生成，具体长 度不确定
     */
    appSec: string;
    /**
     * 请求报文
     * 请求报文( base64 加密 -utf-8)
     * 数据结构是INonVehicle/IVehicle
     */
    data: string;
}